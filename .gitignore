#local
.claude/
CLAUDE.local.md
.claude/settings.local.json
CLAUDE.md
notes/
CONTEXT.md

# Rust 
/target/
**/*.rs.bk
Cargo.lock          # binary crate: remove this line once the app ships; libraries commit it

# OS / editor noise
.DS_Store
*.swp
.idea/
.vscode/

# Leash runtime artifacts (traces & snapshots are per-run local state)
/runs/
*.leash-trace
