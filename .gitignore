# rust
/target/
**/*.rs.bk
Cargo.lock          # binary crate: remove this line once the app ships; libraries commit it

# os / editor noise
.DS_Store
*.swp
.idea/
.vscode/

# leash runtime artifacts (traces and snapshots are per-run local state)
/runs/
*.leash-trace
